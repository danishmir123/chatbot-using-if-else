# chatbot-using-if-else
This is a simple rule-based chatbot built using Python. It uses basic if-elif-else conditions and loops to simulate a conversation. The bot responds to user inputs like “hello”, “how are you”, and “bye”. It handles case-insensitive input using .lower() and runs until the user types “bye” to exit.  This project helps understand basic concepts of:
